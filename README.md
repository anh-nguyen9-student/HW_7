# HW_4
# Program language is Python
# The code is running on vscode 
#
#I'm going to the terminal in the VSCODE
#locate where the file is and type in python3 HW_4.py BOARD_STRING

#Input Handling
#The program reads the board configuration from sys.argv[1].
#If no argument is provided, it prints a usage message and exits.
#Board Initialization
#The input string is converted into a list to represent the
